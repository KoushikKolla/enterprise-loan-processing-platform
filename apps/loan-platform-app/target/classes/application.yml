spring:
  datasource:
    url: jdbc:mysql://localhost:3307/enterprise_loan_db
    username: elpp
    password: elpp123

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080